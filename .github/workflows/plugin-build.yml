name: Build Clonada Plugin

on:
  push:
    branches: [main]
    paths:
      - 'plugin/**'
      - '.github/workflows/plugin-build.yml'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-14]
        include:
          - os: windows-latest
            artifact: clonada-windows
          - os: macos-14
            artifact: clonada-macos

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install pkg-config zeromq

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        run: |
          vcpkg install zeromq:x64-windows-static

      - name: Configure CMake (macOS)
        if: runner.os == 'macOS'
        working-directory: plugin
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Configure CMake (Windows)
        if: runner.os == 'Windows'
        working-directory: plugin
        shell: pwsh
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release "-DCMAKE_TOOLCHAIN_FILE=$env:VCPKG_INSTALLATION_ROOT\scripts\buildsystems\vcpkg.cmake" -DVCPKG_TARGET_TRIPLET=x64-windows-static

      - name: Build
        working-directory: plugin
        run: cmake --build build --config Release --parallel

      - name: Package artifacts (macOS)
        if: runner.os == 'macOS'
        working-directory: plugin
        run: |
          mkdir -p dist
          cp -r build/Clonada_artefacts/Release/VST3/Clonada.vst3 dist/ 2>/dev/null || true
          cp -r build/Clonada_artefacts/Release/AU/Clonada.component dist/ 2>/dev/null || true
          cp -r build/Clonada_artefacts/Release/Standalone/Clonada.app dist/ 2>/dev/null || true

      - name: Package artifacts (Windows)
        if: runner.os == 'Windows'
        working-directory: plugin
        shell: bash
        run: |
          mkdir -p dist
          cp -r build/Clonada_artefacts/Release/VST3/Clonada.vst3 dist/ 2>/dev/null || true
          cp -r build/Clonada_artefacts/Release/Standalone/Clonada.exe dist/ 2>/dev/null || true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: plugin/dist/
          retention-days: 7
